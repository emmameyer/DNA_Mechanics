twist analysis is different for circular dsDNA because there is a "twist step" between the ligated ends, so these files here work for circular dsDNA instead of linear.

intact_bps_h_analysis.py: finds the avergae helical repeat (bp/turn) of only the intact base pairs of a circular dsDNA, averaged over just the last 10 ns
